<!--
 * @Author: error: error: git config user.name & please set dead value or install git && error: git config user.email & please set dead value or install git & please set dead value or install git
 * @Date: 2023-10-26 16:34:17
 * @LastEditors: error: error: git config user.name & please set dead value or install git && error: git config user.email & please set dead value or install git & please set dead value or install git
 * @LastEditTime: 2023-10-27 19:10:34
 * @FilePath: \i-study\accounts\views\list.ejs
 * @Description: 这是默认设置,请设置`customMade`, 打开koroFileHeader查看配置 进行设置: https://github.com/OBKoro1/koro1FileHeader/wiki/%E9%85%8D%E7%BD%AE
-->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Account List</title>
</head>
<body>
    <div>
        <div>
            <form method="post" action="/logout">
                <button type="submit">Logout</button>
            </form>
        </div>
        <hr />
        <h2>account list</h2>
        <h2><a href="/account/create">account</a></h2>
        <hr />
        <div>
            <% accounts.forEach(item =>{ %>
                <div>
                    <div><%= moment(item.time).format('YYYY-MM-DD') %></div>
                </div>
                <div>
                    <div><%= item.title %></div>
                </div>
                <div>
                    <div><%= item.account %></div>
                </div>
                <div>
                    <a href="/account/<%= item.id %>" class="delBtn">Delete</a>
                </div>
            <% }) %>
        </div>
    </div>
</body>
    <script>
        let delBtns = document.querySelectorAll('.delBtn');
        delBtns.forEach(item => {
            item.addEventListener('click',function(e) {
                if(confirm('Are you sure you want to delete?'))
                    return true;
                else
                    e.preventDefault();
            })
        });
    </script>
</html>